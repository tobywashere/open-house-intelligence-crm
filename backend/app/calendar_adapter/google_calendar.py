"""Google Calendar adapter — STRETCH GOAL ONLY.

Implement the same three functions as local_calendar (free_slots, has_conflict,
to_ics/export) against the Google Calendar API. Do NOT start this until both
demo flows work end-to-end on the GB10. The demo must never depend on venue
internet or OAuth working live.
"""

raise NotImplementedError("Stretch goal — use local_calendar for the demo.")
